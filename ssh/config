# Since the first obtained value for each parameter is used, more
# host-specific declarations should be given near the beginning of the file,
# and general defaults at the end.

# A single '*' as a pattern can be used to provide global defaults for all
# hosts.
Host *
  # Indicates that ssh should hash host names and addresses when they are
  # added to ~/.ssh/known_hosts
  HashKnownHosts yes

  # Specifies the UNIX-domain socket used to communicate with the
  # authentication agent
  # IdentityAgent ~/.1password/agent.sock

  # On macOS, specifies whether the system should search for passphrases in
  # the user's keychain when attempting to use a particular key
  # UseKeychain yes
